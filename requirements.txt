# Pinned to a verified working environment: conda env "photoacoustic", Python 3.11,
# macOS/Apple Silicon, CPU-only JAX. Verified 04 Sep 2026 — jwave smoke test passed
# (scripts/smoke_test.py). See IMPLEMENTATION_LOG.md Stage 1.

numpy==2.4.6
scipy==1.17.1
matplotlib
scikit-image==0.26.0

jax==0.4.38
jaxlib==0.4.38
jaxdf==0.2.8
jwave==0.2.1

torch==2.14.0

pytest==9.1.1

# k-wave-python intentionally not installed — fallback only, not needed since jwave is verified
# working.
