# Planned dependencies — not yet installed or version-pinned against a working environment.
# Versions below are placeholders reflecting reasonably current releases as of the architecture
# plan (Sep 2026); pin exact versions once a working environment is actually set up and tested.

numpy
scipy
matplotlib

# Forward acoustic-wave simulation — pick one, do not implement FDTD from scratch:
jwave          # JAX-based; preferred if JAX/GPU setup is already available
# k-wave-python  # alternative, MATLAB-k-Wave-derived; use if jwave setup proves difficult

torch          # U-Net reconstruction model
scikit-image   # SSIM/PSNR metrics

pytest         # for tests/ once implementation exists
