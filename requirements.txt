# Planned dependencies — not yet installed or version-pinned against a working environment.
# Versions below are placeholders reflecting reasonably current releases as of the architecture
# plan (Sep 2026); pin exact versions once a working environment is actually set up and tested.

numpy
scipy
matplotlib

# Forward acoustic-wave simulation — verified choice, do not implement FDTD from scratch:
jwave          # JAX-based, pure Python, no external binary. Verified (Sep 2026) to have documented
               # support for photoacoustic initial-value-problem simulation + time-reversal
               # reconstruction in homogeneous media — see ARCHITECTURE.md Section 5.
# k-wave-python  # fallback only: real, pip-installable without MATLAB, but downloads compiled
               # C++/CUDA binaries on first run. Use only if jwave setup is genuinely blocked.

torch          # U-Net reconstruction model
scikit-image   # SSIM/PSNR metrics

pytest         # for tests/ once implementation exists
